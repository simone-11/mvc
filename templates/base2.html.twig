<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset('img/glider.svg') }}">       
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>
    <body>
        <header class="site-header" style="background-image: url({{ asset('img/background2.jpg') }})">
            <span class="site-title">Min Symfony webbplats</span>
        </header>
        <nav class="nav">
            <ul>
                <li><a href="{{ path('home') }}">Home</a></li>
                <li><a href="{{ path('about') }}">About</a></li>
                <li><a href="{{ path('report') }}">Report</a></li>
                <li><a href="{{ path('lucky_number') }}">Lucky number</a></li>
            </ul>
        </nav>
        <main class="luckypage">{% block body %}{% endblock %}</main>
        <footer class="footer" style="background-image: url({{ asset('img/sunset.jpg') }})">{% block footer %}<a href="https://dbwebb.se/kurser/mvc-v2">Kursen på dbwebb</a>{% endblock %}</footer>
    </body>
</html>

